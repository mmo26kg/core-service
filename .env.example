# Environment variables
# HTTP server port
PORT=3000

# Database connection (example for Postgres + Sequelize)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=core_service
